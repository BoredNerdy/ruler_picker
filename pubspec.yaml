name: ruler_picker_bn
description: Flutter package for ruler type value picker. You can get horizontal and vertical ruler view with the package.
version: 0.0.6
homepage: https://borednerdy.com
repository: https://github.com/BoredNerdy/ruler_picker

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  scrollable_positioned_list:  ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
